# Exploratory Story: Three Published Journey Projections

## Source

- Journey: `nautilus-harness`
- Story id: `ce082594`
- Mode: Explorer Mode

## Continuous Thickening Narrative

Builder must preserve the complete cross-body architecture and staged return contract. The first deliverable is Mirror Journey Projection Contract v1 plus fixtures, expected Operational JSON, black-box security and atomicity scenarios, executable installed-runtime probe and RETURN-CONTRACT.md. It must specify Journey resolution by ID, canonical confinement, namespace isolation, rejection of absolute paths, traversal and symlink escape, schema validation, temporary write plus atomic replace, last-valid preservation, snapshot IDs, source revisions, manifests, extension-facing stable API, deterministic Ariad rebuild after lifecycle mutations and explicit rebuild, and zero model invocation during publication. Ariad documents and lifecycle state remain mutation truth; operational.json is the canonical published read model only. Tactical references the Operational snapshot it interpreted. Strategic references Operational and optional Tactical snapshots. Staleness is observable, not silently repaired. A chat request may explicitly update one altitude or all; Operational routes to deterministic Ariad, Tactical and Strategic route to Pi with Mirror context. No persona owns publication. No private prompts, responses, transcripts, raw reasoning or secrets enter evidence. The separate Mirror session must complete TDD, CI, release tag, central publication, production backup/migration/update and probe evidence. Nautilus must not simulate that return. After the gate passes, initialize independent Git repositories with baseline commits for agentic-protocol and mirror-extension, then deliver Protocol schemas, Method profile, Pi-visible extension skill and publisher, Harness readers and end-to-end snapshot-coherence validation.

## Current Exploratory Story

Three published Journey projections with Ariad as Operational authority

## Narrative Summary

The preferred transfer strategy is one complete end-to-end Builder handoff for the whole three-projection solution, not separate Mirror and Nautilus handoffs. The handoff should preserve the full architecture while encoding a mandatory staged execution: Builder first materializes and validates the Mirror Core contract spec; a separate Mirror delivery implements, releases and installs it; Nautilus work remains blocked until that production capability is verified; then the same umbrella handoff resumes Protocol, Method, Extension and Harness consumption.

## Last Story Card

One handoff can preserve one causal story without collapsing delivery boundaries. Structure it as an umbrella with Phase A Mirror contract specification, Phase B separate Mirror release train, an external production gate, Phase C Nautilus body implementation, and Phase D end-to-end validation. The Builder must stop at each gate and must not simulate the return from the Mirror release.

## Attractors

- **Published Journey synthesis** (`proposed`)
  - An explicit Navigator command asks Pi, operating with Mirror context, to generate a candidate Tactical and/or Strategic reading. A deterministic Mirror/Nautilus publisher validates and atomically writes a versioned projection inside the selected Journey. Harness only reads that published projection. Operational sources remain canonical evidence; the JSON is canonical for the currently published derived reading, not canonical truth about the Journey.

## Experiment Proposal

**Consumer-driven Mirror release return probe** (`proposed`)

Before implementing Mirror Core, Builder materializes Mirror Journey Projection Contract v1, representative Ariad fixture data, expected Operational JSON, black-box security and atomicity scenarios, an executable probe against the installed Mirror CLI/API, and RETURN-CONTRACT.md. The separate Mirror release session must make that probe pass through TDD, CI, versioned release and production installation. On return, Nautilus reruns the unchanged probe against the installed version; Protocol, Method, Extension and Harness work remains blocked until it passes.

## What Changed Through Exploration

This section should preserve the evolution of the exploration: the original question, the meaningful pivots, the corrections that changed the story, and the current point of promotion. If this document was generated from a short runtime summary, Builder should ask the Navigator whether more conversation evidence must be folded in before roadmap work starts.
