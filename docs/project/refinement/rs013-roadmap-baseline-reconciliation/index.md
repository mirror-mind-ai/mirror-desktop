[< Refinement Workbench](../index.md)

# RS013 — Roadmap baseline reconciliation

## Framing

Reconcile the delivered `mirror-desktop` roadmap, refinement history and derived Operational surface so they describe one coherent completed baseline and provide a neutral entry boundary for the next product vision.

## Desired Outcome

CV-001 through CV-007 are represented consistently as delivered; stale historical child statuses and Workbench captures are resolved through explicit existing evidence; no active delivery or refinement focus remains; and the roadmap invites a new `mirror-desktop` exploration without preselecting CV-008.

## Boundaries

- Journey authority is exactly `mirror-desktop`.
- The execution authority is [Baseline Reconciliation Plan](../../roadmap/baseline-reconciliation-plan.md).
- Refinement remains file-first. Legacy SQLite Workbench state is not inspected, reconciled or dual-written.
- Existing completion evidence may reconcile status but may not be rewritten into fabricated historical validation.
- Generated projections are rebuilt through the released Mirror runtime, not hand-edited.
- No application behavior, release version, push, publication, tag, website mutation, updater endpoint, Mirror identity, credentials, conversation, Journey content or app data is changed.

## Change Requests

- [CR026 — Reconcile delivered baseline and open the next product horizon](cr026-reconcile-delivered-baseline-and-open-next-product-horizon.md)

CR026 is `in_progress` with Driver `@alissonvale` and Delivery `refinement/rs013-cr026-roadmap-baseline-reconciliation`.
