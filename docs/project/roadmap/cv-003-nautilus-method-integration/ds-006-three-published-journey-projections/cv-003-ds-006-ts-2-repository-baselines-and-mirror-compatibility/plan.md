# Plan — CV-003.DS-006.TS-2

Initialize independent Git repositories for Agentic Protocol and Mirror Extension with baseline commits. Convert Mirror Extension into an installable command-skill requiring Extension API 1.1. Add an isolated compatibility diagnostic and tests proving publish/inspect round-trip through the public `ExtensionAPI.journey_projections` capability. Do not implement Tactical or Strategic semantics, install production state or import Mirror internals.

Approval and Navigator validation are delegated to the Driver under the authorized accelerated cadence.
