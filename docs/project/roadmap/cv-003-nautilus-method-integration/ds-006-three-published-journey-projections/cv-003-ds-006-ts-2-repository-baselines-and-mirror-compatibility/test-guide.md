# Test Guide — CV-003.DS-006.TS-2

Run the extension tests through the released Mirror source environment. Pass when the diagnostic publishes and inspects an extension-owned synthetic projection, reports contract 1.0/API 1.1, and extension production source imports only `memory.extensions.api.ExtensionAPI`. Confirm both new repositories are clean after coherent commits.
