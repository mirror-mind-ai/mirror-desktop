# Implementation

Reused the bounded native-ID checkpoint grammar behind a dedicated projection namespace. Pi completion now carries exact user/assistant/leaf evidence; Harness projection and deterministic Mirror messages commit idempotently without using text equality as authority.
