# Implementation

A complete Pi turn found after interruption advances bounded Pi/Harness evidence model-free and leaves only the missing Mirror checkpoint pending. The existing specific Mirror repair action now revalidates dedicated authority and writes deterministic messages idempotently without provider invocation.
