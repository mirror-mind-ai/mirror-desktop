# Implementation

Added correlation schema `0.2.0` and pure/Tauri preflight checks binding the exact Journey, thread, active generation, activation receipt, Pi session/file and dedicated Mirror conversation before staging or process creation. Late results retain run/turn/generation coordinates.
