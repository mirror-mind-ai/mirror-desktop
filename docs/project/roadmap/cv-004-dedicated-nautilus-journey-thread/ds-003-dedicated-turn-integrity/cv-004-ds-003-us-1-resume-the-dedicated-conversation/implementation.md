# Implementation

Journey restoration reads only the active generation's exact Pi JSONL, projects complete native turns once in branch order and keeps incomplete tails inert. Dedicated resume uses no conversation picker or external activity import.
